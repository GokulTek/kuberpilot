apiVersion: v1
kind: Service

metadata:
  name: kubepilot-service

spec:
  selector:
    app: kubepilot-backend

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: NodePort
